#!/bin/bash

# install phpBB dependencies
cd /tmp/src/phpBB
php ../composer.phar install

# move the application source
mv /tmp/src/phpBB/* $HOME/

chmod g+w $HOME/cache
chmod g+w $HOME/store
chmod g+w $HOME/files
chmod g+w $HOME/images/avatars/upload/ 
